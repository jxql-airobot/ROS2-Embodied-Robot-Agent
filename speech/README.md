# Speech 模块（规划）

> 状态：**规划中**。目标是把语音作为 Agent 的一种输入模态。

## 流程

```
语音输入 → ASR → 文本任务 → Agent 规划 → 机器人执行
```

示例：

> 用户："找到桌上的水杯"
> 机器人：语音转文本 → Agent 理解 → 视觉定位 → 导航/操作执行

## 阶段

1. ASR 文本转写（中英）接入 Agent 任务入口
2. TTS 反馈与多轮语音交互

## 约束

- 语音模块与 Agent 通过文本解耦，不侵入规划核心
- 不绑定单一 ASR 供应商
